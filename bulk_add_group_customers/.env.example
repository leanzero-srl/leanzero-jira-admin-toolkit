# Jira Cloud (Basic auth). CLOUD_API_TOKEN is base64("email:apiToken"),
# same convention as the other the target site scripts in this repo.
CLOUD_BASE_URL=https://your-site.atlassian.net
CLOUD_API_TOKEN=base64_of_email_colon_apitoken
